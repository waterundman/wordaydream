# ============================================
# Wordaydream Harmony 构建环境变量 (TEMPLATE)
# ============================================
# 复制本文件为 .env.harmony, 填入鸿蒙部署的 LLM Proxy URL:
#   cp .env.harmony.example .env.harmony
#
# 此文件仅在 `npm run build:harmony` (vite build --mode harmony) 时加载.
# Web 端 `npm run build` 不读取此文件, 不影响 Web 端行为.
#
# vite.config.ts 在 harmony 模式下会把 VITE_LLM_PROXY_URL_HARMONY
# 静态注入为前端的 VITE_LLM_PROXY_URL, 前端代码 0 改动.

# ============================================
# v0.1.0-harmony Stage 4: 独立 LLM Proxy URL
# ============================================
# 鸿蒙 ArkWeb 加载 rawfile 协议, 跨域调用 LLM Proxy.
# 真机调试时填开发机局域网 IP, 不要用 localhost (真机的 localhost 指向设备本身).
# 生产部署时填华为云 FunctionGraph / APIG / ECS 公网域名.
#
# 示例:
#   - 真机调试:  http://192.168.1.100:3001/api/llm
#   - 生产部署:  https://api.hapogo.com/api/llm
VITE_LLM_PROXY_URL_HARMONY=http://192.168.1.100:3001/api/llm

# ============================================
# 其它可选 VITE_ 变量 (与 .env 一致, 在此覆盖)
# ============================================
# VITE_LLM_PROVIDER=deepseek
# VITE_LLM_MAX_TOKENS=2048
# VITE_LLM_TEMPERATURE=0.7
# VITE_LLM_RETRY_ATTEMPTS=3
# VITE_LLM_TIMEOUT_MS=30000
# VITE_LLM_GRAYSCALE=100
