# Wordaydream v1.5.3 NEXT-VERSION-DIRECTION

**版本**: v1.5.3 (下一版)
**日期**: 2026-07-10
**工期**: 2-3 天 (前端代码 + 配置文件 + 用户 runbook 完整, 真实执行 1 周)
**起点 posterior**: 0.99+ (v1.5.2 终点承接)
**终点 posterior 目标**: 0.99+ (持平, 用户验证系统真实执行 + 主页深化是延展)
**沙箱可执行**: 0% -> 95% (需 4 API key + Netlify 账号 + Supabase 配置)

---

## 1. v1.5.2 沉淀 (4 合同 全部 PASS, 30 合同总计)

**4 NEW 合同 v1.5.2 (Stage 1-4)**:
- Contract 27 (Stage 1 D-3): 主题切换 (3 主题 light/dark/sepia) — tokens.css + ThemeProvider + ThemeSwitcher + useSettingsStore.theme
- Contract 28 (Stage 2 D-2): 阅读时长统计 — useReadingTimeTracker hook + HeroSection 德文 + useSettingsStore.totalSecondsToday/lastSessionDate
- Contract 29 (Stage 3 D-1): 滚动进度条 — ScrollProgressBar 顶部 3px + rAF + a11y + reduced-motion
- Contract 30 (Stage 4 P2_1): 函数化推广 3 service llm 路径 — grammarDetector + difficultyEvaluator + glossAdapter

**总计**: 30 合同 100% PASS (29 HARD + 1 SOFT), tsc 0 errors, vitest 177/177.

**关键沉淀**:
- persist v3 -> v4 migrate 链 (theme + llm + difficulty + totalSecondsToday + lastSessionDate 透传)
- 6 LLM provider selector 三分支 (mock/heuristic/llm)
- Hero 区域视觉节奏 (clamp + 大 CTA + 60/40 split + 渐变背景)
- 0 breaking change, 0 new dependencies, 0 UI emoji (除 1 预存沿用)

---

## 2. 4 阻塞点 P0 (用户验证系统, v1.5.3 兑现)

v1.5.1 文档化但未执行, v1.5.3 用户执行, posterior 持平 0.99+.

### 2.1 P0_1: Netlify 真实部署 (8 步骤)
1. 注册 Netlify 账号
2. 创建 Site (绑定 GitHub repo)
3. 配置环境变量 (VITE_LLM_PROVIDER / VITE_LLM_PROXY_URL / 6 VITE_LLM 字段)
4. 触发首次部署
5. 验证部署 (访问 Netlify URL)
6. 配置自定义域名 (可选)
7. 验证 Edge Function (curl POST /api/llm-proxy)
8. 验证 SSE 流式 (curl /api/llm-proxy?action=stream)

**工期**: 0.5 天 (用户手动执行)
**沙箱状态**: 0% 可执行 (需 Netlify 账号)
**后验**: 真实 URL 可访问 + Edge Function 返回 200 + SSE 流式正常

### 2.2 P0_2: 3 API key 真实配置 (15 步骤)
1. 注册 OpenAI 账号 + 获取 OPENAI_API_KEY
2. 注册 Anthropic 账号 + 获取 ANTHROPIC_API_KEY
3. 注册 DeepSeek 账号 + 获取 DEEPSEEK_API_KEY
4. 配置 Netlify env: VITE_LLM_PROVIDER=openai
5. 配置 Netlify env: OPENAI_API_KEY=sk-...
6. 配置 Netlify env: ANTHROPIC_API_KEY=sk-ant-...
7. 配置 Netlify env: DEEPSEEK_API_KEY=sk-...
8. 重新部署 Netlify (env 生效)
9. 验证 OpenAI 端点 (curl -X POST /api/llm-proxy)
10. 验证 Anthropic 端点 (切换 provider)
11. 验证 DeepSeek 端点 (切换 provider)
12. 5 句德文 run 验证 Contract 30 (grammarDetector llm 路径)
13. 5 句德文 run 验证 Contract 30 (difficultyEvaluator llm 路径)
14. 5 句德文 run 验证 Contract 30 (glossAdapter llm 路径)
15. 灰度路由真实跑分 (VITE_LLM_GRAYSCALE=50, 5 句德文 50% openai / 50% 默认)

**工期**: 1 天 (用户手动执行)
**沙箱状态**: 0% 可执行 (需 3 API key)
**后验**: 3 provider 全部 200 + 5 句德文端到端正常 + 灰度路由按权重分配

### 2.3 P0_3: Lighthouse 5 项真实跑分 (5 步骤)
1. 触发 lighthouse.yml (每周一 cron 或手动 dispatch)
2. 验证 Performance >= 90 (5% buffer)
3. 验证 Accessibility >= 95
4. 验证 Best Practices >= 95
5. 验证 SEO >= 90 + PWA >= 90

**工期**: 0.5 天 (CI 自动)
**沙箱状态**: 0% 可执行 (需 Lighthouse CI runtime)
**后验**: 5 项评级 >= 阈值, 截图归档

### 2.4 P0_4: Playwright 4 场景真实 E2E (6 步骤)
1. 触发 playwright.yml (microsoft/playwright-github-action@v1 自动安装浏览器)
2. 4 projects: chromium / firefox / webkit / mobile-chrome
3. 场景 1: 主页加载 (Hero + ThemeSwitcher + ScrollProgressBar)
4. 场景 2: 主题切换 (light -> dark -> sepia, 验证 <html data-theme>)
5. 场景 3: 阅读模式 (useReadingTimeTracker 累计 + 5 句德文)
6. 场景 4: PWA install (beforeinstallprompt + 离线模式)

**工期**: 0.5 天 (CI 自动)
**沙箱状态**: 0% 可执行 (需 Chromium / Firefox / WebKit)
**后验**: 4 场景全部 PASS + 4 截图归档 (debug_shots_v153/)

---

## 3. 用户认证 P1 (新方向)

### 3.1 Supabase Auth 集成
- 依赖: `@supabase/supabase-js` ^2.x (新增 1 个依赖, 0 breaking change)
- 集成: src/features/auth/services/supabaseClient.ts + useAuthStore (Zustand persist)
- 流程: 注册 -> 邮箱验证 -> 登录 -> 同步

### 3.2 Email + OAuth (Google/GitHub)
- Email magic link (无密码登录, 沙箱友好)
- OAuth 跳转 (Supabase 控制台配置)
- Token 持久化 (Zustand persist + localStorage)

### 3.3 多设备同步
- 同步字段: reading progress (totalSecondsToday) + settings (theme/llm/difficulty) + collection
- 冲突解决: last-write-wins (Supabase Realtime 订阅)
- 离线优先: 离线时写本地, 在线时同步

**工期**: 1.5 天 (前端代码 + Supabase 配置)
**沙箱状态**: 50% 可执行 (前端代码, 需 Supabase 账号)
**后验**: 注册 + 登录 + 多设备同步正常

---

## 4. i18n P1 (新方向)

### 4.1 react-i18next 集成
- 依赖: `react-i18next` ^13.x + `i18next` ^23.x + `i18next-browser-languagedetector` ^8.x (3 新依赖)
- 集成: src/i18n/index.ts + src/i18n/locales/{de,en,zh}.json
- 默认语言: 浏览器 navigator.language (de-DE -> de, en-US -> en, zh-CN -> zh)

### 4.2 德文 / 英文 / 中文 三语
- de: 原版 (de.json, 沿用 v1.5.2 硬编码文案)
- en: 翻译 100% UI 文案
- zh: 翻译 100% UI 文案
- 切换: SettingsPanel 注入 LanguageSwitcher (3 选项)

### 4.3 词卡释义自动翻译
- 集成 LLM 翻译 (沿用 v1.5.2 3 service 框架)
- 用户切换语言时, 已存词卡释义自动翻译
- 翻译缓存: localStorage 避免重复调用

**工期**: 1.5 天 (翻译 + i18n 集成)
**沙箱状态**: 80% 可执行 (前端代码, 翻译文案需准备)
**后验**: 3 语切换正常 + 词卡释义自动翻译正常

---

## 5. 词卡复习 P1 (TS-FSRS 沿用)

### 5.1 spaced repetition 调度
- 依赖: `ts-fsrs` ^5.4.1 (v0.5.0 已引入, 沿用)
- 集成: src/features/review/services/fsrsScheduler.ts (沿用 v0.5.0)
- 升级: 支持德文 (CEFR 5 级 + 词性 + 复合词)

### 5.2 复习卡片 UI
- 组件: ReviewCard.tsx + ReviewSession.tsx
- 交互: 翻面 (点击) + 评分 (1-4 按钮) + 下一张
- 动画: 翻面 3D transform + 0 emoji

### 5.3 复习统计 + 推送提醒
- 统计: 今日复习数 + 连续天数 + 总掌握率
- 提醒: 浏览器 Notification API (用户授权后) + 主页 ReviewReminder 组件
- 提醒文案: "Du hast N Karten zu überprüfen" (德文)

**工期**: 1 天
**沙箱状态**: 95% 可执行 (前端代码, 浏览器通知需用户授权)
**后验**: 复习流程正常 + 统计 + 提醒正常

---

## 6. 收藏夹 P2

### 6.1 单词收藏 + 短语收藏 + 段落收藏
- 数据结构: useCollectionStore (Zustand persist) + types
- 单元: 单词 (Lemma) + 短语 (Phrase) + 段落 (Passage)
- 标签: 用户自定义 tag (e.g. "工作", "日常", "考试")

### 6.2 收藏夹管理 UI
- 组件: CollectionList.tsx + CollectionDetail.tsx + CollectionFilter.tsx
- 操作: 添加 (单词右键) + 删除 (列表项按钮) + 编辑 (tag) + 搜索
- 视图: 列表 / 卡片 / 时间线 3 种切换

### 6.3 收藏夹导出 (JSON / CSV)
- JSON: 完整数据结构 (含 tag + 创建时间 + 来源 passage)
- CSV: 简化版 (单词 + 释义 + 标签 + 创建时间)
- 导入: 解析 JSON 合并到 store (冲突处理: last-write-wins)

**工期**: 1 天
**沙箱状态**: 100% 可执行 (纯前端)
**后验**: 收藏 + 管理 + 导出 / 导入 正常

---

## 7. 数据导出 P2

### 7.1 用户数据 (设置 / 进度 / 收藏) 导出 JSON
- 范围: useSettingsStore + useMemoryStore + useCollectionStore + useAnalyticsStore
- 格式: JSON (含 version 字段, 方便后续 import 兼容)
- 时间: 含 createdAt / updatedAt

### 7.2 导入 JSON 恢复
- 解析: 校验 JSON schema + version 兼容
- 合并: 新数据 + 旧数据 (冲突 last-write-wins)
- 备份: 导入前自动备份当前数据到 "backup-YYYY-MM-DD.json"

### 7.3 隐私保护
- 本地优先: 0 服务端, 全部 localStorage (或 IndexedDB 大数据)
- 用户控制: 导出时用户确认 + 导入时用户确认
- 加密 (可选): 用户可设置密码加密 JSON (AES-GCM)

**工期**: 0.5 天
**沙箱状态**: 100% 可执行 (纯前端)
**后验**: 导出 / 导入正常 + 隐私保护

---

## 8. 横评矩阵 (8 方向横评, top 4 = 4 阻塞点 + 用户认证)

| 排名 | 编号 | 方向 | 加权 | 沙箱 | 决策 |
|------|------|------|------|------|------|
| 1 | P0_1 | Netlify 真实部署 | 1.00 | 0.00 | P0 (用户执行) |
| 2 | P0_2 | 3 API key 真实配置 | 1.00 | 0.00 | P0 (用户执行) |
| 3 | P0_3 | Lighthouse 真实跑分 | 0.95 | 0.00 | P0 (CI 自动) |
| 4 | P0_4 | Playwright 真实 E2E | 0.95 | 0.00 | P0 (CI 自动) |
| 5 | P1_1 | 用户认证 (Supabase) | 0.80 | 0.50 | P1 主推 |
| 6 | P1_2 | i18n (3 语) | 0.70 | 0.80 | P1 主推 |
| 7 | P1_3 | 词卡复习 (TS-FSRS) | 0.65 | 0.95 | P1 主推 |
| 8 | P2_1 | 收藏夹 + 数据导出 | 0.55 | 1.00 | P2 主推 |

**决策依据**:
- P0 (4 阻塞点): 用户验证系统, posterior 加分, 必须用户执行
- P1 (用户认证 / i18n / 词卡): 用户感知强, 加权 0.65-0.80
- P2 (收藏夹 + 数据导出): 用户感知中等, 加权 0.55

---

## 9. 合同预测: v1.5.3 新增 6-8 合同

**P0 真实执行 (4 合同, 用户验证)**:
- Contract 31: Netlify 真实部署 (URL 可访问 + Edge Function 200)
- Contract 32: 3 API key 真实配置 (OpenAI + Anthropic + DeepSeek 5 句德文 端到端)
- Contract 33: Lighthouse 5 项真实跑分 (>= 阈值)
- Contract 34: Playwright 4 场景真实 E2E (chromium / firefox / webkit / mobile-chrome 4 截图)

**P1 NEW (3-4 合同, 新增功能)**:
- Contract 35: 用户认证 (Supabase Auth + Email + OAuth + 多设备同步)
- Contract 36: i18n (react-i18next + 3 语 + 词卡释义自动翻译)
- Contract 37: 词卡复习 (TS-FSRS + ReviewCard + 复习统计 + 推送提醒)

**P2 NEW (1-2 合同)**:
- Contract 38: 收藏夹 (单词 + 短语 + 段落 + 标签 + 导出 JSON/CSV)
- Contract 39: 数据导出 (设置 + 进度 + 收藏 + 隐私保护)

**总计**: 6-8 NEW 合同, 累计 36-39 合同.

---

## 10. Bayesian 计划

**posterior 演化**:
- v1.5.2 终点: 0.99+ (Stage 1-4 累积)
- v1.5.3 起点: 0.99+ (承接)
- v1.5.3 Stage 1 (P0 真实执行): 0.99+ -> 0.99+ 持平 (用户执行 + 验证, posterior 加分)
- v1.5.3 Stage 2 (P1 用户认证 + i18n + 词卡): 0.99+ -> 0.99+ 持平 (新功能 + 集成复杂度对冲)
- v1.5.3 Stage 3 (P2 收藏 + 导出 + 收尾): 0.99+ -> 0.99+ 持平 (纯前端 + 集成复杂度对冲)
- v1.5.3 终点: 0.99+ 持平

**沙箱可执行**:
- v1.5.2 沙箱 95% 可执行 (静态分析 + 单元测试)
- v1.5.3 沙箱 0% -> 95% (用户执行 4 阻塞点, 前端代码完整)

**工期**:
- 前端代码: 2-3 天
- 用户执行 4 阻塞点: 1 周 (用户节奏)
- 总周期: 1-2 周

---

## 11. 优先级总览

| 优先级 | 方向 | 工期 | 沙箱 | 决策 |
|--------|------|------|------|------|
| P0 | 4 阻塞点真实执行 (Netlify + 3 API key + Lighthouse + Playwright) | 用户 1 周 | 0% | 必须 |
| P1 | 用户认证 (Supabase) | 1.5 天 | 50% | 主推 |
| P1 | i18n (3 语) | 1.5 天 | 80% | 主推 |
| P1 | 词卡复习 (TS-FSRS) | 1 天 | 95% | 主推 |
| P2 | 收藏夹 + 数据导出 | 1.5 天 | 100% | 主推 |
| P3 | 1 预存 emoji 修复 (CompoundWordDisplay.tsx:94) | 0.1 天 | 100% | 收尾 |
| P3 | 1 预存 emoji 修复 (遗留) | 0.1 天 | 100% | 收尾 |

**v1.5.3 完整任务清单** (按 Stage 1-3 划分):
- Stage 1: P0 真实执行 (用户) + 1 预存 emoji 修复
- Stage 2: P1 用户认证 + i18n + 词卡复习
- Stage 3: P2 收藏夹 + 数据导出 + 收尾

**v1.5.3 沙箱限制**:
- 4 阻塞点用户执行 (沙箱 0%)
- Supabase 配置 (沙箱 50%)
- i18n 翻译文案 (沙箱 80%, 需用户 review)
- 词卡复习 (沙箱 95%, 浏览器通知需用户授权)
- 收藏夹 + 数据导出 (沙箱 100%)

---

## 12. 风险与缓解

### 12.1 风险 1: 4 阻塞点用户执行未完成
- 影响: posterior 0.99+ -> 0.95 下修
- 缓解: 文档化 runbook (OPERATIONS.md) + 沙箱内 static analysis 兜底

### 12.2 风险 2: Supabase 集成复杂度
- 影响: 用户认证延期 + 后端依赖
- 缓解: 沙箱 50% 可执行 (前端代码), Supabase 配置用户执行

### 12.3 风险 3: i18n 翻译工作量大
- 影响: de/en/zh 三语 100% 翻译 1.5 天
- 缓解: react-i18next 自动化 + 翻译文案由用户 review

### 12.4 风险 4: 词卡复习 TS-FSRS 升级
- 影响: 现有 v0.5.0 沿用代码需扩展
- 缓解: 沿用 v0.5.0 baseline, 仅扩展德文支持

### 12.5 风险 5: 1 预存 emoji 仍遗留
- 影响: 0 UI 影响, 但违反硬约束
- 缓解: v1.5.3 Stage 1 收尾修复

---

## 13. 与 R10 (v1.5.1) / R11 (v1.5.2) 对比

| 维度 | R10 (v1.5.1) | R11 (v1.5.2) | R12 (v1.5.3) |
|------|--------------|--------------|--------------|
| 核心方向 | 4 阻塞点 runbook + Hero-First | 主页深化 + 函数化推广 | 用户验证 + 用户认证 + i18n + 词卡 + 收藏 |
| 工期 | 1 天 | 1 天 | 2-3 天前端 + 1 周用户 |
| 合同增量 | 22 -> 25 (+3) | 25 -> 30 (+5) | 30 -> 36-39 (+6-8) |
| 测试增量 | 0 -> 163 (+0) | 163 -> 177 (+14) | 177 -> 200+ (+23+) |
| 主页视觉 | Hero 60/40 + clamp + 大 CTA | + 主题 + 时长 + 进度条 | (沿用, 0 视觉变化) |
| 失败教训 | 0 真实部署 | 0 真实部署 (沿用) | 0 真实部署 (待用户) + 1 subagent 派发拼写 |
| 收获沉淀 | runbook 34 步骤 | persist v3 -> v4 migrate 链 | Supabase + i18n + TS-FSRS 升级链 |

**v1.5.3 显著变化**:
- 4 阻塞点真实执行 (用户)
- 用户认证 (Supabase) - 第一个后端集成
- i18n (3 语) - 第一个多语言支持
- 词卡复习 (TS-FSRS) - 第一个 spaced repetition UI
- 收藏夹 + 数据导出 - 第一个用户数据所有权
